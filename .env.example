# Example environment variables file
# Copy this to .env and fill in with your actual credentials
# Never commit the actual .env file to version control

# Weights & Biases API key
WANDB_API_KEY=your_wandb_api_key_here

# Set this to define default project name (can be overridden with --project flag)
WANDB_PROJECT=chess-theme-classifier